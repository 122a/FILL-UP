import 'package:flutter/material.dart';

class FillUpAI extends StatelessWidget {
  const FillUpAI({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Fill Up AI"),
      ),
    );
  }
}
